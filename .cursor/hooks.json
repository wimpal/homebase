{
  "version": 1,
  "_comment": "Generated by project-control-heim/scripts/install_agent_rules.py. Re-run it to update. Hooks fail open by design: failClosed is deliberately not set. On Windows the installer writes .cmd shims; on Unix it writes .sh.",
  "hooks": {
    "sessionStart": [
      { "command": "./.cursor/hooks/pc-session-start.cmd", "timeout": 20 }
    ],
    "stop": [
      { "command": "./.cursor/hooks/pc-session-stop.cmd", "timeout": 40, "loop_limit": 2 }
    ],
    "preToolUse": [
      { "command": "./.cursor/hooks/pc-guard-writes.cmd", "timeout": 10 }
    ]
  }
}
